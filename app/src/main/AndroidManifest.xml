<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.roar71.assignment1">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".KitchenAidActivity"
            android:theme="@style/AppTheme.KitchenAidBar"
            android:label="@string/k_app_name"></activity>
        <activity
            android:name=".LilytopActivity"
            android:theme="@style/AppTheme.Lilypadbar"
            android:label="@string/l_app_name"/>
        <activity android:name=".FlatUiKitActivity"
            android:theme="@style/AppTheme.FlatUiKitBar"/>
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>